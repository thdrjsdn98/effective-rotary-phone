# 전기기사 필기 요약집 - 작업 규칙

이 프로젝트를 수정하는 AI(어떤 모델이든)는 아래 규칙을 반드시 따라주세요.

## 0. 프로젝트 구조 (중요 — 소방안전관리자 프로젝트와 다름)
이 프로젝트는 **하나의 앱이 아니라, 과목별로 독립된 6개의 미니 웹앱 + 허브(root)**로 구성되어 있다.
과목 폴더를 수정할 때는 **그 폴더 안의 index.html / sw.js만** 건드리면 되고, 다른 과목에는 영향을 주지 않는다.

| 폴더 | 과목 | sw.js의 CACHE_NAME 접두어 |
|---|---|---|
| `/` (루트) | 과목 선택 허브 | `electric-circuit-cache-vX` ⚠️ 아래 "알려진 이슈" 참고 |
| `circuit-theory/` | 회로이론 | `electric-circuit-cache-vX` |
| `control-eng/` | 제어공학 | `electric-control-cache-vX` |
| `magnetism/` | 전기자기학 | `electric-magnetism-cache-vX` |
| `power-eng/` | 전력공학 | `electric-power-cache-vX` |
| `machinery/` | 전기기기 | `electric-machinery-cache-vX` |
| `electrical-standards/` | 전기설비기준 | `electric-standards-cache-vX` |

### ⚠️ 알려진 이슈
- 루트 `sw.js`의 CACHE_NAME이 `electric-circuit-cache-v1.0.2`로 되어 있어 `circuit-theory/`와 **이름이 겹친다**. 루트 캐시 이름은 `electric-hub-cache-vX` 등 고유한 이름으로 분리하는 걸 권장한다 (다음 루트 수정 시 반영).
- 버전 표기 방식이 폴더마다 다르다 (`v1`, `v2`, `v6`, `v1.0.2` 등 혼재). 새로 캐시 버전을 올릴 때는 **그 폴더에 기존에 쓰던 숫자 형식을 유지**하며 1씩 올린다 (예: `v6` → `v7`).

## 1. 버전 관리
- 수정한 과목 폴더의 `index.html` 안 `버전: VX.X` 문구를 **0.0.1씩** 올린다.
- 아직 changelog(업데이트 내역) 패널이 없는 과목이 대부분이다. 만약 소방안전관리자 프로젝트처럼 표지에 업데이트 내역 패널을 추가하고 싶다면, 사용자에게 먼저 확인 후 추가한다 (임의로 구조를 바꾸지 않는다).
- 여러 과목을 동시에 수정했다면, 수정한 과목들의 버전을 각각 개별적으로 올린다 (전체를 하나의 버전으로 묶지 않는다).

## 2. 서비스워커 캐시 버전 (매우 중요!)
- 과목 폴더의 `index.html`, `script.js`, `style.css`, `questions.js` 등 그 과목 안의 파일을 수정했다면, **그 과목의 `sw.js`에 있는 `CACHE_NAME`도 반드시 1씩 올린다.**
- 이걸 안 올리면 PWA로 설치한 사용자 브라우저가 예전 캐시된 파일을 계속 보여줄 수 있다.
- 건드리지 않은 다른 과목의 `sw.js`는 그대로 둔다.

## 3. 파일 구조 공통 사항
- 각 과목 폴더는 `index.html`(문제/이론 UI), `script.js`(로직), `style.css`, `questions.js`(문제 데이터), `sw.js`(캐시), `manifest.json`(PWA 설정)로 구성된다.
- 다이어그램이 있다면 순수 인라인 SVG 스타일을 유지한다 (외부 라이브러리 없이).
- 색상은 하드코딩하지 않고 `style.css`에 정의된 CSS 변수를 최대한 사용한다.

## 4. 마무리 시 항상 할 것
1. 수정 완료 후 프로젝트 폴더 전체(루트부터)를 다시 zip으로 압축한다.
2. 파일명 형식: `VX.X.X(업데이트내용요약).zip`
   - 여러 과목을 수정했다면 요약에 과목명을 포함한다. 예: `V1.0.2(회로이론_전력공학_오탈자_수정).zip`
   - 버전 번호는 점(`.`)으로, 요약 안 띄어쓰기는 밑줄(`_`)로 표기한다.
3. 사용자에게 파일을 제공(present)한다.

---
### 다른 채팅방/다른 AI로 옮길 때 사용법
이 zip 파일을 업로드한 뒤, 아래처럼 요청하면 규칙이 이어집니다:

> "압축 파일 안의 PROJECT_RULES.md를 참고해서 이 규칙대로 작업해줘. [원하는 수정 내용]"
